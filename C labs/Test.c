#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void)
{
    int i = 42;
    char z = 'c';
    double d = 3.14;
    
    printf("The integer is %d, the char is %c and the double is %f", i, z, d);
}